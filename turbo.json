{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["WCA_CLIENT_ID", "WCA_CLIENT_SECRET", "BETTER_AUTH_URL", "BETTER_AUTH_SECRET", "DATABASE_URL", "RESEND_API_KEY", "NODE_ENV", "VERCEL_ENV", "NEXT_PUBLIC_BOARDS_URL", "NEXT_PUBLIC_CALENDAR_URL", "AUTH_COOKIE_DOMAIN"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "test": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
